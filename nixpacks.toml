[phases.install]
dependsOn = ["setup"]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm install"
]

[variables]
NODE_OPTIONS = "--dns-result-order=ipv4first"

[start]
cmd = "bash start.sh"